function final_plot = generate_final_plot(alpha, n)
    resulting_vector = zeros(n);
    
    for i = 1 : n - 1
        
    end
end

function piecewise_function = generate_piecewise_function(i, n)
    y = zeros(n);
    y(i) = 1;
    piecewise_function = y;
end